cp .env.example .en# Copy to .env — never commit .env

PORT=5000
AI_SERVICE_URL=http://localhost:5001
DB_PATH=./grievance.db

# Vite dev server default is 5173. Add your Bolt/Replit preview URL
# once you have one, comma-separated.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
